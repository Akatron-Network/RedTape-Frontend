export default function reducer(state, action) {

  switch (action.type) {

    //, case 'GET_DISTRICTS':
    //,   return {
    //,     ...state,
    //,     districts: action.value
    //,   }
  }
}
